library(caret)

#sample(x, size, replace = FALSE, prob = NULL)
# x, valores a seleccionar
# size, cantidad de valores a seleccionar
# replace, muestreo con o si reemplazado, valores repetidos o no
# prob, vector de pesos de probabilidad para seleccionar valores


sample(80:100, size=20, replace = FALSE, prob = NULL)



